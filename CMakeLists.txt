# Note: CMake support is community-based. The maintainers do not use CMake
# internally.
# CMakeLists.txt provided by 4eck-qed

cmake_minimum_required(VERSION 3.5)

project(argon2)

add_subdirectory(include)
#add_subdirectory(src)
